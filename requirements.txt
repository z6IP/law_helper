# Web 框架
fastapi>=0.135.0
uvicorn[standard]>=0.41.0

# 前端
streamlit>=1.40.0

# 配置与校验
pydantic>=2.12.0
pydantic-settings>=2.13.0
python-dotenv>=1.2.0

# 向量库（1.x 起不再依赖 chroma-hnswlib 编译，兼容 Python 3.13 / Windows）
chromadb==1.5.5

# Embedding & Rerank
sentence-transformers==3.2.1

# 稀疏检索
rank-bm25==0.2.2
jieba>=0.42.1

# LLM
openai>=1.50.0

# 会话、限流
starsessions>=2.2.0
fastapi-throttle>=0.1.8

# 文档解析
python-docx>=1.1.2
pymupdf==1.28.0      # PDF 版面解析（GB 19522-2024 等国家标准）

# 模型下载（ModelScope 镜像）
modelscope>=1.20.0